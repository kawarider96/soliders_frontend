/*import { SET_SELECTED_COLUMNS } from '../actions/columnActions';

const initialState = {
  selectedColumns: {
    col1: { label: 'pozíció', value: 'position' },
    col2: { label: 'sztsz', value: 'sztsz' },
    col3: { label: 'teljes név', value: 'fullname' },
    col4: { label: 'járási század', value: 'basedata.squad' },
    col5: { label: 'állománykategória', value: 'type' },
  },
};

export default function columnReducer(state = initialState, action) {
  switch (action.type) {
    case SET_SELECTED_COLUMNS:
      return {
        ...state,
        selectedColumns: action.payload,
      };
    default:
      return state;
  }
}*/